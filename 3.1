def calculator():
    a, b = 20, 10
    op = input("Введи +, -, * або /: ")
    results = {'+': a + b, '-': a - b, '*': a * b, '/': a / b}
    print(f"Результат: {results.get(op, 'Невірна операція!')}")
if __name__ == "__main__":
    calculator()
